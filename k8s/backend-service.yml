apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: deployguard

spec:
  type: NodePort

  selector:
    app: backend-deployguard

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081

